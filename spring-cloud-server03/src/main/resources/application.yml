spring:
  application:
    name: server03
  cloud:
    config:
      uri: http://localhost:7003/
      profile: dev
      label: main
server:
  port: 6003

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
