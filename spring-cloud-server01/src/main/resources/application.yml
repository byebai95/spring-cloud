spring:
  application:
    name: server01
  cloud:
    config:
      uri: http://localhost:7003/
      profile: dev
      label: main

server:
  port: 6001

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false