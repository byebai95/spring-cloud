server:
  port: 7003

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/byebai95/config-repository.git
          username: byebai95
          password: bzz.299616
          search-paths: config
          default-label: main
        bootstrap: true


